{
    "name": "elm-poker",
    "version": "1.0.0",
    "description": "",
    "main": "",
    "scripts": {
        "prepare-dist": "rm -rf dist && mkdir -p dist && cp index.html dist/",
        "watch": "npm run prepare-dist && npx elm-watch hot",
        "build": "npm run prepare-dist && npx elm-watch make --optimize"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "elm-watch": "^1.2.3"
    }
}
