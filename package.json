{
    "name": "elm-poker",
    "version": "1.0.0",
    "description": "",
    "main": "",
    "scripts": {
        "watch": "rm -rf dist && mkdir -p dist && cp index.html dist/ && npx elm-watch hot",
        "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && npx elm-watch make --optimize"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "elm-watch": "^1.2.3"
    }
}
